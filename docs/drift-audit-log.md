# Drift audit log

One line per archived OpenSpec change, written by the `farpost-drift-audit` skill. This is what that skill reads to know what's already been checked — don't skip logging a clean result, silence here would just look like the audit never ran.

| Date | Change | Result | Notes |
|---|---|---|---|
| 2026-07-26 | scaffold-desktop-framing | Clean | Independently re-ran build/lint/test (all pass) and read every file against the literal spec, not just CLI's report. All 8 requirements in `specs/desktop-app-shell/spec.md` verified directly in code. One finding, not against the build: `design.md` Decision 1 and Decision 5 gave contradicting implementation guidance (fixed vs. sticky) — CLI resolved it correctly and flagged it; `design.md` corrected post-hoc so the archived record doesn't preserve the contradiction. Two things intentionally left open per design.md, confirmed still open: right-rail 256px width (verified working with current content, may need tuning later) and the printed-palette-as-web-palette assumption (Robin's explicit sign-off still pending). |
| 2026-07-26 | header-spacing-and-icon-alignment | Clean | Independently re-ran build/lint/test (all pass, 8/8 tests). Verified all six requested changes against real code, including the arithmetic chain: header's real measured height (73.75px, script-measured not hand-calculated) propagated consistently into `DrawerNav`/`RightRail`'s 88.75px clearance (73.75 + the requested 15px), and into `PageOutline`/`globals.css`'s 179px scroll calibration. The one MODIFIED requirement (icon alignment, right-edge to left-edge) synced into `openspec/specs/desktop-app-shell/spec.md`. `Header.test.tsx` correctly asserts both the new alignment classes and the absence of the old ones. CLI touched 3 files not listed in the proposal's Impact section (`PageHeader.tsx`, `PageOutline.tsx`, `globals.css`) as a direct, necessary consequence of the header's height changing from fixed to content-driven — flagged plainly rather than silently expanding scope; audit agrees this was correct judgment, not scope creep. |
| 2026-07-26 | mobile-app-shell | Clean | Independently re-ran build/lint/test (all pass, 12/12 tests). Verified the mobile header consolidation directly in code: `Header.tsx` now owns both the mobile and `xl:` render paths, `DrawerNav.tsx`/`RightRail.tsx`'s floating mobile bars are gone, `RightRail.tsx` correctly dropped `"use client"`. `globals.css`'s `.signed-in-only`/`.signed-out-only`/`[data-signed-in]` toggle logic checked by hand and is correct. Confirmed CLI's claimed temporary `playwright-core` install left no trace (`package.json`, lockfile, `node_modules` all checked directly). The `MobileNavProvider` placement bug CLI found and fixed (`Header` needs `useMobileNav()` inside the provider) is real and correctly fixed. New `mobile-app-shell` capability synced into `openspec/specs/mobile-app-shell/spec.md`. One cosmetic-only nit not worth a fix: `pt-[73.75px] xl:pt-[73.75px]` and `top-[73.75px] xl:top-[73.75px]` now carry a redundant `xl:` variant equal to the base value — harmless, not flagged back to CLI. |
| 2026-07-26 | settings-page | Clean | Independently re-ran build/lint/test (all pass, 19/19 tests). Confirmed no theme control exists anywhere in code, matching the explicit non-goal. `--font-scale`'s `html`-not-`body` application and its non-self-referential `1rem` reasoning checked and correct. Reduced motion's two real effects verified directly: `DrawerNav`'s slide transition is CSS-scoped via a `motion-safe-transition` marker class (not a blanket override — deliberately narrow), `PageOutline.handleSelect` reads `.reduce-motion` fresh at click time since `scrollIntoView`'s explicit `behavior` can't be governed by CSS `scroll-behavior`. `SettingsBootstrap` correctly carries no theme/session logic. New `settings-page` capability synced into `openspec/specs/settings-page/spec.md`. The font-scale/header-height risk Chat flagged during scoping is correctly logged in `docs/issues.md` under Open, with the literal handoff text preserved per the file's own convention — not fixed, as instructed. |
| 2026-07-27 | scaffold-fastify-backend | Clean | Independently re-ran build/typecheck/test (all pass, 3/3), and directly confirmed both real-infrastructure claims rather than trusting the report: `docker ps` shows Farpost's Postgres (5435) and Vocare's (5434) coexisting cleanly; `psql` against the real container confirms `drizzle.__drizzle_migrations` genuinely exists (empty, since the schema is genuinely empty — no `.sql` file was generated, matching an empty `_journal.json`). One real spec correction made before archiving, same category as `scaffold-desktop-framing`'s fixed/sticky fix: the CORS requirement assumed an unconfigured origin would get no header at all; `@fastify/cors` with a static-string origin (Vocare's own real pattern) always echoes the *configured* origin regardless of the request — corrected the requirement text to describe the real, still-correct security mechanism (header value mismatch, not header absence) rather than defend the original wrong assumption. `dist/` present in a working-tree check was from Chat's own `npm run build` moments earlier, not a CLI leftover — confirmed gitignored either way. |
| 2026-07-27 | wire-better-auth | Clean | Independently re-ran build/typecheck/test in both `api/` (8/8) and `web/` (build/lint/test all clean, 19/19). Verified directly in the real database, not just trusted the report: all 5 tables exist (`user`, `session`, `account`, `verification`, `membership`), `user` has no Vocare-specific columns, `membership` matches `core-user-model.md`'s shape exactly (unconstrained `role` text), and a real `rgsamways@gmail.com` row with `email_verified: true` exists from the actual test send. Read `auth.ts`, `fastify-plugin.ts`, `auth-client.ts`, and `AuthBootstrap.tsx` directly — all match the design (no `additionalFields`/`databaseHooks`, both real Fastify gotchas intact, `AuthBootstrap` is reactive via `useSession()` not a one-time check). The "404 on redirect" CLI mentioned is a real, expected artifact (no callback destination exists until `sign-in-and-account-pages`), not a defect — the actual verify request returned a real `302` with a session cookie, confirmed in `auth.test.ts`. Left the test user row in the dev database per Chat's own call — useful for testing `sign-in-and-account-pages` next, not a real one to worry about. |
| 2026-07-27 | sign-in-and-account-pages | Clean | Independently re-ran build/lint/test (all pass, 25/25). Independently verified CLI's bug-fix claim against a third source, not just trusted it: grepped Vocare's real frontend and confirmed it uses the exact same `` `${window.location.origin}/account` `` absolute-callbackURL pattern CLI says it copied — the design.md correction is real, not invented. Read `SignInForm.tsx`, both pages' `useSession()`/`isPending` redirect guards, and `Header.tsx`'s desktop parity fix directly — all match spec. `Header.test.tsx` correctly extended with the same structural-toggle assertion pattern used for mobile. Two capabilities synced: new `sign-in-and-account-pages` and an ADDED requirement on `desktop-app-shell` (session-conditional icon, closing the parity gap from `mobile-app-shell`). This is the last change in the currently-drafted pipeline — nothing queued behind it as of archiving. |
| 2026-07-27 | core-building-schema | Clean | Read every schema file directly (`property-schema.ts`, `building-schema.ts`, `unit-schema.ts`, `asset-schema.ts`, `stake-schema.ts`, the reconciled `membership-schema.ts`, `role-type-schema.ts`) against the four archived spec files' literal field lists — every field, type, nullability, CHECK constraint, and index matches exactly, including the negative requirement ("building SHALL NOT include owner_name/owner_email/owner_phone", confirmed absent and confirmed a raw-SQL insert against that column fails). Independently re-ran the full `api/` test suite (37/37 passing) rather than trusting the build report, and read the test files themselves (`stake-schema.test.ts`, `asset-schema.test.ts`, `building-schema.test.ts`, `membership-schema.test.ts`) to confirm they exercise the real database (no mocking) against the exact scenarios the specs describe — unclaimed stake with null user_id, subject_type=unit, invalid-status/invalid-verification-method rejection, FK-restrict on Property/Building delete, duplicate nfc_tag_id rejection, active-role-uniqueness with revoke-then-regrant. Confirmed the `membership` reconciliation (task 1.2) was additive by diff, not a rewrite. Confirmed `openspec validate --specs --strict` passes clean for all 10 specs including the 4 newly-synced ones. No drift found — nothing to log in `docs/issues.md`. |
| 2026-07-27 | job-dispatch-schema | Clean | Read every new schema file directly (`professional-profile-schema.ts`, `dispatch-capability-schema.ts`, `compliance-record-schema.ts`, `job-schema.ts`, `job-notes-schema.ts`, `job-attachment-schema.ts`, `job-cost-breakdown-schema.ts`, `work-request-attempt-schema.ts`, `claim-schema.ts`) and the migration SQL directly against the four archived spec files — every field, CHECK constraint (including the nine-value `job.status`, the `compliance_record.verification_status` vocabulary, and `claim`'s confirmed absence of a `status` column), and FK cascade/restrict rule matches design.md exactly. Verified both real issues CLI reported are genuine, not misreadings: the `geography` quoting bug reproduced and was hand-fixed identically to `core-building-schema`; the `work_request_attempt` partial-index predicate correction (`timeout_at > now()` dropped, `responded_at IS NULL` kept) is real Postgres behavior — partial-index predicates must be immutable and `now()` is STABLE, so the original design.md wording would have failed at `CREATE INDEX` time. The one deliberate deviation from tasks.md's literal wording (`claim.buildingId` wired as a real Drizzle relation, not left unwired like the true polymorphic pairs) is correctly reasoned and documented inline in both `claim-schema.ts` and `relations.ts` — a real single-target nullable FK isn't the same shape as `subject_type`/`subject_id`, so this is a justified correction, not scope drift. Independently re-ran the full `api/` test suite: 66/66 passing across 18 files (29 new tests in 9 new files, not the "38 across 12" the pasted report claimed — that report was visibly garbled in transit, and the real count is what's logged here). Confirmed `openspec validate --specs --strict` passes clean for all 14 specs including the 4 newly-synced ones. No drift found — nothing to log in `docs/issues.md`. One open item carried forward, not a defect: `compliance_record.verification_status` conflates time-based expiry with for-cause revocation, per CLI's own observation — already flagged in design.md as needing Robin's sign-off, not something to silently fix. |
| 2026-07-27 | checklist-schema | Clean | Read every new schema file directly (`checklist-template-schema.ts`, `checklist-template-item-schema.ts`, `checklist-run-schema.ts`, `checklist-result-schema.ts`) and the migration SQL directly against the two archived spec files — every field, the `(name, version)` UNIQUE constraint, both CHECK constraints (`checklist_template.status`, `checklist_run.overall_status`'s invented vocabulary, `checklist_result.condition_status`), and every FK cascade/restrict rule (the one deliberate `CASCADE` on `checklist_result → checklist_run`, `RESTRICT` everywhere else) matches design.md exactly. Confirmed no relation was added between `checklist_run`/`checklist_result` and `job`/`claim`, matching design.md's explicit Non-Goal — checked `relations.ts` directly rather than trusting the report's claim. The `checklist_result.assetId`/`checklistRuns`-on-building-and-membership relation additions are the same well-reasoned real-single-target-FK judgment call as `claimRelations` in `job-dispatch-schema`, correctly documented inline. Independently re-ran the full `api/` test suite: 83/83 passing across 22 files (17 new tests in 4 new files, not the "76" the pasted report claimed — same garbled-in-transit pattern as the prior report; the real count is what's logged here). Confirmed `openspec validate --specs --strict` passes clean for all 16 specs including the 2 newly-synced ones. No drift found — nothing to log in `docs/issues.md`. Grounding note: this change's design.md checked the real archived `checklist_run.py` model before speccing (not because docs disagreed, but to confirm the full-design doc's "replaced"/"new" claims were accurate) and confirmed the old `ChecklistResult` really was a bare `{asset_id, passed, notes}` sub-document — holds up. |
| 2026-07-27 | event-log-schema | Clean | Read `event-schema.ts`, `event-recipient-schema.ts`, and the generated migration SQL (`0009_event_log_schema.sql`) directly against the archived spec's two requirements — every field, both CHECK constraints (`subject_type`, `urgency`, `delivery_status`), and both FK rules (`event_recipient.event_id` CASCADE, both `*_user_id` columns RESTRICT to `user.id`) match exactly. Independently re-queried the live dev DB via `information_schema.columns`/`pg_constraint`/`pg_indexes` rather than trusting the report's `psql \d` claim — confirmed `actor_user_id`/`recipient_user_id` are genuinely `text` at the database level, not just in Drizzle's TypeScript layer, and all 6 named indexes (including both partial indexes) exist as specified. Confirmed `relations.ts` only wires `event` ↔ `event_recipient`, with `subjectType`/`subjectId` correctly left unrelated (poly pattern, same as `asset`/`stake`/`job`), matching design.md. Independently re-ran the full `api/` test suite: 92/92 passing across 24 files (9 new tests in 2 new files, not the "14" the pasted report claimed — same garbled-in-transit pattern as `job-dispatch-schema`/`checklist-schema`'s reports; read both test files and confirmed 9 is the real, correct count covering every scenario in the spec). Confirmed `openspec validate --changes --strict` passed clean before archiving. One follow-up item, not drift: the source design doc's `uuid`-typed `actor_user_id`/`recipient_user_id` correction was applied cleanly in code/migration/DB during the build, but the doc edit itself was deliberately left uncommitted by the builder rather than claimed as their own — committed separately (`3807524`) before archiving this change. No drift found — nothing to log in `docs/issues.md`. |
