// Genuinely empty for now — real tables arrive with `wire-better-auth`.
// This change (`scaffold-fastify-backend`) only proves the Postgres/Drizzle
// migration workflow works end-to-end; it doesn't add app schema.
export {};
